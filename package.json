{
  "name": "kanan-landing-pages",
  "version": "1.0.0",
  "description": "Kanan.co landing pages monorepo - IELTS, GRE, GMAT",
  "private": true,
  "workspaces": [
    "apps/*",
    "shared/*"
  ],
  "scripts": {
    "dev": "echo 'Static HTML version - no dev server needed'",
    "dev:ielts": "echo 'Static HTML version - no dev server needed'",
    "dev:gre": "echo 'Static HTML version - no dev server needed'",
    "dev:gmat": "echo 'Static HTML version - no dev server needed'",
    "build": "echo 'Static HTML version - no build required'",
    "build:workspaces": "echo 'Static HTML version - no build required'",
    "build:ielts": "echo 'Static HTML version - no build required'",
    "build:gre": "echo 'Static HTML version - no build required'",
    "build:gmat": "echo 'Static HTML version - no build required'",
    "preview": "echo 'Static HTML version - open index.html in browser'",
    "lint": "echo 'Static HTML version - no linting needed'",
    "type-check": "echo 'Static HTML version - no type checking needed'"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}